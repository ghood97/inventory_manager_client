{{#each inventories as |inventory|}}
  <tr data-inventory-id={{inventory.id}} data-product-id={{inventory.product.id}}>
    <th scope="row">{{inventory.id}}</th>
    <td>{{inventory.product.name}}</td>
    <td>${{inventory.price}}</td>
  </tr>
{{/each}}
