{{#each products as |product|}}
  <tr data-product-id={{product.id}} data-toggle="modal" data-target="#add-product-modal">
    <th scope="row">{{product.id}}</th>
    <td>{{product.name}}</td>
    <td>${{product.retail_price}}</td>
  </tr>
{{/each}}
